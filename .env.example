# Host port exposed by Docker Compose.
PORT=8080
# Use 127.0.0.1 when a reverse proxy runs directly on the same host.
# Use 0.0.0.0 when a cloud load balancer must reach this port.
BIND_ADDRESS=0.0.0.0
